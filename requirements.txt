requests>=2.31.0
yfinance>=0.2.36
# v37h (10.05.2026): lxml ist optional-dep von yfinance fuer earnings_dates
# HTML-Parsing. Ohne lxml -> ticker.earnings_dates raised ModuleNotFoundError
# -> Backtester's _fetch_historical_earnings_dates returnt [] silent ->
# earnings_blackouts leer -> Earnings-Filter wirkungslos (Bot tradet
# durch Earnings-Tage). Carlos hat das via earnings_symbols_covered=0
# im Backtest-Output entdeckt 10.05.2026.
lxml>=5.0.0
reportlab>=4.1.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
jinja2>=3.1.3
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0
pyotp>=2.9.0
qrcode>=7.4.2
slowapi>=0.1.9
aiosqlite>=0.19.0
scikit-learn>=1.4.0
numpy>=1.26.0
anthropic>=0.42.0
vaderSentiment>=3.3.2
google-api-python-client>=2.100.0
google-auth>=2.25.0
psutil>=5.9.0
# IBKR-Integration (W2/W3) — siehe app/ibkr_client.py + app/ibkr_contract_resolver.py
ib_insync>=0.9.86
nest_asyncio>=1.6.0
# Pairs Trading (Bot Nr. 5) — Engle-Granger Cointegration + ADF
statsmodels>=0.14.0
# tzdata fuer ib_insync IANA-Zeitzonen ('US/Eastern' im Order-Parsing).
# slim-python-images haben /usr/share/zoneinfo nicht; entweder apt-tzdata
# (siehe Dockerfile) ODER PyPI-tzdata reichen einzeln, beide=Belt+Suspenders.
tzdata>=2024.1
# Error-Tracking (v37g, 08.05.2026) — Sentry fuer Real-Money-Phase Visibility.
# Stack-Traces + Context fuer ib_insync-Drifts, Order-Reject-Edge-Cases, etc.
# Pushover bleibt fuer Trading-Alerts. Feature-Flag: config.json sentry.enabled.
# DSN via env var SENTRY_DSN (nicht im Repo, nicht in Logs).
sentry-sdk[fastapi]>=2.0.0

